# Shared

This folder stores small constants and helper code that both the frontend and
backend can use.

Examples:

- Socket event names
- Default room names
- Shared validation rules

Keep this folder framework-free. Do not put React components or server-only code
here.
